server:
  port: 8086

spring:
  application:
    name: history-service

  datasource:
    url: jdbc:mysql://localhost:3306/revplay_history?createDatabaseIfNotExist=true
    username: root
    password: yourpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: cmV2cGxheV9zdXBlcl9zZWNyZXRfa2V5X2Zvcl9qd3RfMjAyNF92ZXJ5X2xvbmc=

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true 

feign:
  client:
    config:
      default:
      connectTimeout: 5000
      readTimeout: 5000
circuitbreaker:
  enabled: true